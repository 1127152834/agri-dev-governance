# 冲突处理参考

## 默认规则

- 任务池状态优先
- 成员目录为派生视图
- 模块文档目录为派生视图

## 常见冲突

### 成员 `tasks.md` 未更新

按照任务池状态，把任务移动到对应栏目。

### 成员 `status.md` 过期

按任务池当前状态更新当前主任务与状态说明。

### 模块目录状态落后

根据任务池阶段，确认模块目录是否需要补齐对应文档入口或更新阶段说明。

## 无法判断时

如果任务池本身缺失记录、状态矛盾，必须先报告冲突，不继续自动同步。
